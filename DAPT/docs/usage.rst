.. _usage:

Usage
=====

Once you have `installed <install.html>`_ DAPT and have verified that it's installed correctly you can start setting it up for actual parameter runs.  Below are links showing how to use the project.


